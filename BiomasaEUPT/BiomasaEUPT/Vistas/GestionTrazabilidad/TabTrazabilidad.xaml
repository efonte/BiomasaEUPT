<UserControl x:Class="BiomasaEUPT.Vistas.GestionTrazabilidad.TabTrazabilidad"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BiomasaEUPT.Vistas.GestionTrazabilidad"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid>
        <!--local:Trazabilidad x:Name="ucTrazabilidad" Margin="10 10 10 10" /-->
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="tbCodigo" Grid.Row="0" Grid.Column="0"
                 materialDesign:HintAssist.Hint="Código" HorizontalAlignment="Center"
                 MaxLength="10" FontSize="15" FontWeight="Bold">
        </TextBox>
        <Border Grid.Row="1" Grid.Column="0" Background="{StaticResource MaterialDesignBackground}" Margin="10 10 0 10">
            <GroupBox materialDesign:ColorZoneAssist.Mode="PrimaryMid" Header="Almacenamiento" Style="{StaticResource MaterialDesignGroupBox}" >
                <ScrollViewer x:Name="svAlmacenamiento" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                </ScrollViewer>
            </GroupBox>
        </Border>
        <Border Grid.Row="1" Grid.Column="1" Background="{StaticResource MaterialDesignBackground}" Margin="10 10 0 10">
            <GroupBox materialDesign:ColorZoneAssist.Mode="PrimaryMid" Header="Elaboración" Style="{StaticResource MaterialDesignGroupBox}" >
                <ScrollViewer x:Name="svElaboracion" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                </ScrollViewer>
            </GroupBox>
        </Border>
        <Border Grid.Row="1" Grid.Column="2" Background="{StaticResource MaterialDesignBackground}" Margin="10 10 10 10">
            <GroupBox materialDesign:ColorZoneAssist.Mode="PrimaryMid" Header="Venta" Style="{StaticResource MaterialDesignGroupBox}" >
                <ScrollViewer x:Name="svVenta" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                </ScrollViewer>
            </GroupBox>
        </Border>
    </Grid>
</UserControl>
