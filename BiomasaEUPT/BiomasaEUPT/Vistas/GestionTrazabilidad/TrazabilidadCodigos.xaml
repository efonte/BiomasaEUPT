<UserControl x:Class="BiomasaEUPT.Vistas.GestionTrazabilidad.TrazabilidadCodigos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BiomasaEUPT.Vistas.GestionTrazabilidad"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <GroupBox Grid.Row="0" Grid.RowSpan="2" materialDesign:ColorZoneAssist.Mode="PrimaryMid" Header="" Style="{StaticResource MaterialDesignGroupBox}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{StaticResource MaterialDesignBackground}" Margin="0 0 5 0">
                    <GroupBox x:Name="gbAlmacenamiento" materialDesign:ColorZoneAssist.Mode="PrimaryMid"
                              Header="Almacenamiento" Style="{StaticResource MaterialDesignGroupBox}"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <TreeView x:Name="tvAlmacenamiento">
                        </TreeView>
                    </GroupBox>
                </Border>
                <Border Grid.Column="1" Background="{StaticResource MaterialDesignBackground}" Margin="5 0 5 0">
                    <GroupBox x:Name="gbElaboracion" materialDesign:ColorZoneAssist.Mode="PrimaryMid"
                              Header="Elaboración" Style="{StaticResource MaterialDesignGroupBox}">
                    </GroupBox>
                </Border>
                <Border Grid.Column="2" Background="{StaticResource MaterialDesignBackground}" Margin="5 0 0 0">
                    <GroupBox x:Name="gbVenta" materialDesign:ColorZoneAssist.Mode="PrimaryMid"
                              Header="Venta" Style="{StaticResource MaterialDesignGroupBox}">
                    </GroupBox>
                </Border>
            </Grid>
        </GroupBox>
        <TextBox x:Name="tbCodigo" Grid.Row="0"
                 materialDesign:HintAssist.Hint="Código" HorizontalAlignment="Right" Width="100" Margin="0 5 10 0"
                 MaxLength="10" FontSize="15" FontWeight="Bold" Foreground="White" HorizontalContentAlignment="Right">
        </TextBox>
    </Grid>
</UserControl>
